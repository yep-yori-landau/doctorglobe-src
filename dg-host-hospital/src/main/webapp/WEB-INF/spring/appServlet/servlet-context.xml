<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- THYMELEAF: Template Resolver for email templates -->
	<beans:bean id="emailTemplateResolver"
		class="org.thymeleaf.templateresolver.ClassLoaderTemplateResolver">
		<beans:property name="prefix" value="mail/" />
		<beans:property name="templateMode" value="HTML5" />
		<beans:property name="characterEncoding" value="UTF-8" />
		<beans:property name="order" value="1" />
		<!-- Template cache is true by default. Set to false if you want -->
		<!-- templates to be automatically updated when modified. -->
		<beans:property name="cacheable" value="true" />
	</beans:bean>

	<beans:bean id="webtemplateResolver"
		class="org.thymeleaf.templateresolver.ServletContextTemplateResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".html" />
		<beans:property name="templateMode" value="HTML5" />
		<!-- Template cache is set to false (default is true). -->
		<beans:property name="order" value="2" />
		<beans:property name="cacheable" value="false" />
	</beans:bean>

	<beans:bean id="templateEngine" class="org.thymeleaf.spring3.SpringTemplateEngine">
		<beans:property name="templateResolvers">
			<beans:set>
				<beans:ref bean="emailTemplateResolver" />
				<beans:ref bean="webtemplateResolver" />
			</beans:set>
		</beans:property>
		<beans:property name="additionalDialects">
			<beans:set>
				<beans:bean class="org.thymeleaf.extras.springsecurity3.dialect.SpringSecurityDialect" />
			</beans:set>
		</beans:property>
	</beans:bean>
	
	<beans:bean id="viewResolver"
		class="org.thymeleaf.spring3.view.ThymeleafViewResolver">
		<beans:property name="templateEngine" ref="templateEngine" />
		<beans:property name="characterEncoding" value="UTF-8" />
	</beans:bean>
		
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />
	
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	<resources mapping="/webjars/**" location="classpath:/META-INF/resources/webjars/" />

	<beans:bean id="messageSource"
		class="org.springframework.context.support.ResourceBundleMessageSource"
		p:basename="messages" />
		
	<!-- Url -->
	<beans:bean id="urlProperties"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<beans:property name="location"
			value="classpath:url-${dgEnvTarget}.properties" />
		<beans:property name="ignoreResourceNotFound" value="false" />
		<beans:property name="searchSystemEnvironment" value="true" />
		<beans:property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
	</beans:bean>
	
	<beans:bean id="contextSource" class="com.dg.hospital.service.RestUrlService">
			<beans:property name="healthcareproviderUrl" value="${url.healthcareproviderBaseUrl}" />			
			<beans:property name="equipmentUrl" value="${url.equipmentBaseUrl}" />
			<beans:property name="healthcareproviderEquipmentUrl" value="${url.healthcareproviderEquipmentBaseUrl}" />
			<beans:property name="accreditationUrl" value="${url.accreditationBaseUrl}" />
			<beans:property name="healthcareproviderAccreditationUrl" value="${url.healthcareproviderAccreditationBaseUrl}" />
			<beans:property name="amenityUrl" value="${url.amenityBaseUrl}" />
			<beans:property name="healthcareproviderAmenityUrl" value="${url.healthcareproviderAmenityBaseUrl}" />
			<beans:property name="cuisineUrl" value="${url.cuisineBaseUrl}" />
			<beans:property name="healthcareproviderCuisineUrl" value="${url.healthcareproviderCuisineBaseUrl}" />
			<beans:property name="insuranceUrl" value="${url.insuranceBaseUrl}" />
			<beans:property name="healthcareproviderInsuranceUrl" value="${url.healthcareproviderInsuranceBaseUrl}" />
			<beans:property name="countryUrl" value="${url.countryBaseUrl}" />
			<beans:property name="stateUrl" value="${url.stateBaseUrl}" />
			<beans:property name="cityUrl" value="${url.cityBaseUrl}" />
			<beans:property name="chainUrl" value="${url.chainBaseUrl}" />
			<beans:property name="ratingcompaniesUrl" value="${url.ratingcompaniesBaseUrl}" />
			<beans:property name="healthcareproviderRatingsUrl" value="${url.healthcareproviderRatingsBaseUrl}" />
			<beans:property name="healthcareproviderLanguageUrl" value="${url.healthcareproviderLanguageBaseUrl}" />						
			<beans:property name="healthcareproviderPhotoUrl" value="${url.healthcareproviderPhotoBaseUrl}" />
			<beans:property name="healthcareprovidertypeUrl" value="${url.healthcareprovidertypeBaseUrl}" />
			<beans:property name="healthcareproviderHealthcareprovidertypeUrl" value="${url.healthcareproviderHealthcareprovidertypeBaseUrl}" />
			<beans:property name="featureUrl" value="${url.featureBaseUrl}" />
			<beans:property name="healthcareproviderunitUrl" value="${url.healthcareproviderunitBaseUrl}" />			
			<beans:property name="healthcareproviderUnitFeatureUrl" value="${url.healthcareproviderUnitFeatureBaseUrl}" />
			<beans:property name="specialtyUrl" value="${url.specialtyBaseUrl}" />
			<beans:property name="specialtyTreatmentUrl" value="${url.specialtyTreatmentBaseUrl}" />
			<beans:property name="healthcareproviderSpecialtyUrl" value="${url.healthcareproviderSpecialtyBaseUrl}" />
			<beans:property name="treatmentUrl" value="${url.treatmentBaseUrl}" />
			<beans:property name="methodUrl" value="${url.methodBaseUrl}" />			
			<beans:property name="healthcareproviderSpecialtyTreatmentMethodUrl" value="${url.healthcareproviderSpecialtyTreatmentMethodBaseUrl}" />
			<beans:property name="healthcareproviderSpecialtyTreatmentMethodPractitionerUrl" value="${url.healthcareproviderSpecialtyTreatmentMethodPractitionerBaseUrl}" />
			<beans:property name="healthcareproviderSpecialtyTreatmentMethodPractitionerStatUrl" value="${url.healthcareproviderSpecialtyTreatmentMethodPractitionerStatBaseUrl}" />
			<beans:property name="practitionerUrl" value="${url.practitionerBaseUrl}" />
			<beans:property name="practitionerLanguageUrl" value="${url.practitionerLanguageBaseUrl}" />
			<beans:property name="practitionertypeUrl" value="${url.practitionertypeBaseUrl}" />
			<beans:property name="attestationsUrl" value="${url.attestationsBaseUrl}" />
			<beans:property name="attestationsPractitionerUrl" value="${url.attestationsPractitionerBaseUrl}" />
			<beans:property name="aclRolesUrl" value="${url.aclRolesBaseUrl}" />
			<beans:property name="statsUrl" value="${url.statsBaseUrl}" />
			<beans:property name="userUrl" value="${url.userBaseUrl}" />
			<beans:property name="baseUrl" value="${url.hostBaseUrl}" />
			<beans:property name="mediaUrl" value="${url.mediaBaseUrl}" />			
			<beans:property name="healthcareproviderPractitionerUrl" value="${url.healthcareproviderPractitionerBaseUrl}" />
			<beans:property name="uploadPath" value="${url.uploadPath}" />
			<beans:property name="imageResourceUrl" value="${url.imageResourceUrl}" />
			<beans:property name="corporatecompanyInsuranceUrl" value="${url.corporatecompanyInsuranceBaseUrl}" />
	</beans:bean>	
	
	<beans:bean id="restService" class="com.dg.hospital.service.RestService">
    	<beans:property name="restUrlService" ref="contextSource"></beans:property>
    </beans:bean>
    
    <beans:bean id="hospitalService" class="com.dg.hospital.service.HospitalService">
    	<beans:property name="restUrlService" ref="contextSource"></beans:property>
    </beans:bean>
    
    <beans:bean id="hospitalSpecialtiesService" class="com.dg.hospital.service.HospitalSpecialtyService">
    	<beans:property name="restUrlService" ref="contextSource"></beans:property>
    </beans:bean>
    
    <beans:bean id="hospitalPractitionerService" class="com.dg.hospital.service.HospitalPractitionerService">
    	<beans:property name="restUrlService" ref="contextSource"></beans:property>
    </beans:bean>    
    
    <beans:bean id="mailService" class="com.dg.hospital.service.MailService">
		<beans:property name="mailSender" ref="mailSender" />
		<beans:property name="restUrlService" ref="contextSource"></beans:property>
		<beans:property name="mailFrom" value="${mail.from}"></beans:property>
	</beans:bean>        
	
    <beans:bean
		class="org.springframework.web.context.support.ServletContextAttributeExporter">
		<beans:property name="attributes">
			<beans:map>				
				<beans:entry key="hostbaseurl" value="${url.hostBaseUrl}" />			
			</beans:map>
		</beans:property>
	</beans:bean>
    
    <!-- Mail config -->
	<beans:bean id="mailSender"
		class="org.springframework.mail.javamail.JavaMailSenderImpl">
		<beans:property name="host" value="${mail.server.host}" />
		<beans:property name="port" value="${mail.server.port}" />
		<beans:property name="username" value="${mail.server.username}" />
		<beans:property name="password" value="${mail.server.password}" />
		<beans:property name="javaMailProperties">
			<beans:props>
				<beans:prop key="mail.smtp.auth">${mail.smtp.auth}</beans:prop>
				<beans:prop key="mail.smtp.starttls.enable">${mail.smtp.starttls.enable}</beans:prop>
				<beans:prop key="mail.debug">${mail.debug}</beans:prop>
			</beans:props>
		</beans:property>
	</beans:bean>

	
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	
	<!-- File upload config -->
	<beans:bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="maxUploadSize" value="10000000000" />
	</beans:bean>
	<resources mapping="/doctorglobe-hospital/**" location="${url.filepath}" />
	<resources mapping="/doctorglobe-doctor/**" location="${url.doctorfilepath}" />
	
	<context:component-scan base-package="com.dg.hospital" />
	
	
	
</beans:beans>